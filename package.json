{
  "name": "sonkhipto",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:studio": "pnpm -F @acme/server db:studio",
    "db:migrate": "pnpm -F @acme/server db:migrate",
    "db:generate": "pnpm -F @acme/server db:generate",
    "ios": "pnpm -F @acme/mobile ios",
    "android": "pnpm -F @acme/mobile android"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@acme/typescript-config": "workspace:*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}

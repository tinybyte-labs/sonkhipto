{
  "name": "sonkhipto",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "studio": "pnpm -F @acme/db studio",
    "prisma:studio": "pnpm -F @acme/db studio",
    "prisma:migrate": "pnpm -F @acme/db migrate",
    "prisma:generate": "pnpm -F @acme/db generate",
    "ios": "pnpm -F @acme/mobile ios",
    "android": "pnpm -F @acme/mobile android"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@acme/typescript-config": "workspace:*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
